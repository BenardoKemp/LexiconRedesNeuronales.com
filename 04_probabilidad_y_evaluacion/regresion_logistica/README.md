# Regresión logística

Este ejemplo muestra cómo la regresión logística convierte entradas numéricas en probabilidades usando la función sigmoide.

La salida siempre está entre 0 y 1, lo que permite realizar clasificación binaria.

## Ejecutar

python regresion_logistica.py